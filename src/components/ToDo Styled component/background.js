import styled from 'styled-components';
import img from './images/img-background.jpg';

const Background = styled.div`
background-image: url(${img});
background-size: screen.height;
`;

export default Background